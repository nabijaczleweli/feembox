initSidebarItems({"enum":[["InnerAscii","InnerAscii is string data container which can contain either a owned `SoftAsciiString` or a `SoftAsciiStr` reference into a shared string buffer."],["InnerUtf8","InnerUtf8 is string data container which can contain either a owned `String` or a `str` reference into a shared string buffer."],["SimpleItem",""]],"struct":[["Input","a Input is similar to Item a container data container used in different context's with different restrictions, but different to an Item it might contain characters which require encoding (e.g. encoded words) to represent them"]]});